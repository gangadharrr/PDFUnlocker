{
  "name": "backend-node",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run clean && tsc && npm run build:python",
    "build:python": "pip install -r src/python/requirements.txt && cp -r src/python/ dist/python/ ",
    "start": "node dist/index.js",
    "dev": "npm run build:python && nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^25.0.9",
    "@vercel/node": "^5.5.28",
    "nodemon": "^3.1.11",
    "rimraf": "^6.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/multipart": "^9.4.0",
    "@vercel/python": "^6.4.2",
    "dotenv": "^17.2.3",
    "fastify": "^5.7.1",
    "node-qpdf2": "^6.0.0",
    "pino": "^10.3.0",
    "pino-pretty": "^13.1.3",
    "vercel": "^50.9.1",
    "zod": "^4.3.6"
  }
}
